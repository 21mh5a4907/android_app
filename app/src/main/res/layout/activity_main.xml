<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:background="@color/white">

    <!-- Title Text: Centered -->
    <TextView
        android:id="@+id/title_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Task Manager"
        android:textColor="#2E5AC1"
        android:textSize="40sp"
        android:textStyle="bold"
        android:layout_centerHorizontal="true"
        android:layout_alignParentTop="true"
        android:layout_marginTop="40dp"/>

    <TextView
        android:id="@+id/description_text"
        android:layout_width="432dp"
        android:layout_height="218dp"
        android:layout_below="@id/title_text"
        android:layout_alignParentEnd="true"
        android:layout_marginTop="95dp"
        android:layout_marginEnd="0dp"
        android:fontFamily="sans-serif-condensed-medium"
        android:text="Stay on top of your tasks with Task Manager.Track your deadlines, set notifications, and work together with your team to enhance productivity."
        android:textColor="#384D8C"
        android:textSize="25sp" />

    <ImageView
        android:id="@+id/feature_image"
        android:layout_width="432dp"
        android:layout_height="307dp"
        android:layout_below="@id/description_text"
        android:layout_alignParentEnd="true"
        android:layout_marginTop="-13dp"
        android:layout_marginEnd="16dp"
        android:src="@drawable/images_removebg_preview" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_below="@id/feature_image"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="30dp">

        <!-- Login Button -->
        <Button
            android:id="@+id/login_btn"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="15dp"
            android:layout_weight="1"
            android:backgroundTint="#204394"
            android:text="Login"
            android:textColor="@color/white"
            android:textSize="15dp"
            tools:ignore="TextSizeCheck" />

        <!-- Admin Login Button -->

        <!-- Register Button -->
        <Button
            android:id="@+id/admin_login_btn"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="15dp"
            android:layout_weight="1"
            android:backgroundTint="#204394"
            android:text="Admin Login"
            android:textColor="@color/white"
            android:textSize="15dp"
            tools:ignore="TextSizeCheck" />

        <Button
            android:id="@+id/register_btn"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="15dp"
            android:layout_weight="1"
            android:backgroundTint="#204394"
            android:text="SignUp"
            android:textColor="@color/white"
            android:textSize="15dp"
            tools:ignore="TextSizeCheck" />

    </LinearLayout>

</RelativeLayout>